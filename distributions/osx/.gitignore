# Build artifacts — do not commit
build/

# Local signing credentials — never commit
.env

# macOS system files
.DS_Store

# Swift build artifacts (outside build/)
app/.build/
app/.swiftpm/

# Xcode-related
*.xcodeproj
*.xcworkspace
xcuserdata/

# Node.js (if any local tooling)
node_modules/
