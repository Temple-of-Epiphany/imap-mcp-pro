{
  "name": "IMAP MCP Pro",
  "description": "Comprehensive IMAP email integration with secure account management and connection pooling",
  "version": "2.13.0",
  "author": "Temple of Epiphany",
  "license": "SEE LICENSE IN LICENSE",
  "homepage": "https://github.com/Temple-of-Epiphany/imap-mcp-pro",
  "repository": {
    "type": "git",
    "url": "https://github.com/Temple-of-Epiphany/imap-mcp-pro.git"
  },
  "server": {
    "command": "node",
    "args": ["dist/index.js"],
    "env": {
      "NODE_ENV": "production",
      "CLAUDE_DESKTOP_EXTENSION": "true"
    }
  },
  "user_config": {
    "default_timeout": {
      "type": "integer",
      "title": "Connection Timeout (ms)",
      "description": "Default timeout for IMAP connections in milliseconds",
      "default": 30000,
      "minimum": 5000,
      "maximum": 120000,
      "required": false
    },
    "enable_connection_pooling": {
      "type": "boolean",
      "title": "Enable Connection Pooling",
      "description": "Reuse IMAP connections across operations for better performance",
      "default": true,
      "required": false
    },
    "max_pool_size": {
      "type": "integer",
      "title": "Maximum Pool Size",
      "description": "Maximum number of concurrent IMAP connections per account",
      "default": 5,
      "minimum": 1,
      "maximum": 20,
      "required": false
    },
    "enable_notifications": {
      "type": "boolean",
      "title": "Enable Notifications",
      "description": "Show notifications for new emails (requires IDLE support)",
      "default": false,
      "required": false
    }
  },
  "capabilities": {
    "tools": true,
    "resources": true,
    "prompts": true,
    "notifications": true
  }
}
